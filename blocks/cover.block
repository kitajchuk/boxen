<div class="cover">
    <div class="cover__title -wrap -expt">
        <h1>{title}</h1>
        <h3 class="teal">{description|safe}</h3>
    </div>
    {##
    {.section mainImage}
        <div class="cover__image -wrap">
            <img class="image js-lazy-image" data-img-src="{assetUrl}" data-variants="{systemDataVariants}" data-original-size="{originalSize}" />
        </div>
    {.end}
    ##}
</div>
